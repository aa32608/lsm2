import React from 'react';
import MainApp from '../components/MainApp';

export const metadata = {
  title: 'Tetovo LMS - Home',
  description: 'Listing Management System Home',
};

export default function HomePage() {
  return <MainApp />;
}
